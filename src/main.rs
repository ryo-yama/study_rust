//fn bubble_sort(vecTgt: Vec<i32>) {
//    for i in 
//}

fn main() {
    // 基礎
    basic();
}

fn basic() {
    // 変数の宣言
    let out: i32 = 712;

    // ミュータブルな変数の宣言
    let mut sample: i32 = 10;
    println!("{}", sample);
    sample = 20;
    println!("{}", sample);

    // リテラル
    // 生文字リテラル:エスケープシーケンスもそのまま出力される
}
